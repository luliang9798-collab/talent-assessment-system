# Nginx 配置（Docker 容器内使用）
# 由 docker-compose.yml 挂载到 /etc/nginx/conf.d/default.conf
server {
    listen 80;
    server_name _;

    client_max_body_size 20m;

    # 反向代理到后端容器（服务名 app，compose 网络内可解析）
    location / {
        proxy_pass http://app:3000;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_read_timeout 60s;
        proxy_connect_timeout 10s;
    }
}

# ============================================================
# 启用 HTTPS（可选）：
# 1) 将证书放到 docker/nginx/ssl/ 目录（talent-assessment.crt / .key）
# 2) 取消 docker-compose.yml 中 443 端口与 ssl 卷的注释
# 3) 用以下内容替换上方 server 块：
#
# server {
#     listen 443 ssl http2;
#     server_name _;
#     ssl_certificate     /etc/nginx/ssl/talent-assessment.crt;
#     ssl_certificate_key /etc/nginx/ssl/talent-assessment.key;
#     ssl_protocols TLSv1.2 TLSv1.3;
#     client_max_body_size 20m;
#     location / {
#         proxy_pass http://app:3000;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto https;
#     }
# }
# server {
#     listen 80;
#     server_name _;
#     return 301 https://$host$request_uri;
# }
# ============================================================
