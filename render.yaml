services:
  - type: web
    name: talent-assessment-system
    env: node
    plan: free
    buildCommand: cd backend-nodejs && npm install
    startCommand: cd backend-nodejs && node server-minimal.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
    healthCheckPath: /api/health
